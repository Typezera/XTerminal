#include "user_keywords.h"
#include <stdio.h>

int commands_keyword_user(){
   char buffer_commands_user[40];

   printf(">");
   fgets(buffer_commands_user, sizeof(buffer_commands_user), stdin);

    
}